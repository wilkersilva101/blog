<p style="color: green"><%= notice %></p>

<%= render @article %>

<div>
  <%= link_to "Listar Artigos", articles_path %>
  <% if can? :update, @article %>
    <%= link_to "Editar", edit_article_path(@article) %> |
  <% end %>
  <% if can? :destroy, @article %>
    <%= button_to "Excluir", @article, method: :delete, data: { confirm: 'Tem certeza?' } %>
  <% end %>
</div>
